{
  "max_video_size_mb": {
    "type": "float",
    "description": "允许处理的视频最大体积(MB)",
    "default": 50.0
  },
  "max_gif_duration": {
    "type": "float",
    "description": "生成的动图最大允许时长(秒)",
    "default": 10.0
  },
  "default_scale": {
    "type": "float",
    "description": "默认缩放比例(0.1-1.0)",
    "default": 0.3
  },
  "default_fps": {
    "type": "int",
    "description": "默认帧率",
    "default": 10
  },
  "gif_max_colors": {
    "type": "int",
    "description": "GIF最大颜色数量(2-256)，仅在输出格式为GIF时生效",
    "default": 256
  },
  "output_format": {
    "type": "string",
    "description": "输出格式：GIF (兼容性好), APNG (画质好透明度高), WEBP (体积最小)",
    "enum": ["GIF", "APNG", "WEBP"],
    "default": "GIF"
  },
  "image_size_limit_mb": {
    "description": "图像大小限制 (MB)",
    "type": "int",
    "default": 10,
    "min": 1,
    "max": 100,
    "hint": "允许处理的普通图像文件最大大小，单位MB"
  },
  "gif_size_limit_mb": {
    "description": "GIF大小限制 (MB)",
    "type": "int",
    "default": 15,
    "min": 1,
    "max": 200,
    "hint": "允许处理的GIF动画文件最大大小，单位MB"
  },
  "precheck_file_size_mb": {
    "description": "预检查文件大小限制 (MB)",
    "type": "int",
    "default": 100,
    "min": 10,
    "max": 500,
    "hint": "打开图像前允许读取的最大文件大小，用于提前拦截异常大文件"
  },
  "max_compression_dimension": {
    "description": "压缩最大尺寸 (像素)",
    "type": "int",
    "default": 2048,
    "min": 512,
    "max": 8192,
    "hint": "启用自动压缩时，图像最长边会压缩到不超过该值"
  },
  "max_total_pixels": {
    "description": "GIF总像素限制",
    "type": "int",
    "default": 16000000,
    "min": 250000,
    "max": 100000000,
    "hint": "GIF帧数乘以单帧像素数的安全上限，用于防止解压炸弹"
  },
  "processing_timeout": {
    "description": "处理超时时间 (秒)",
    "type": "int",
    "default": 30,
    "min": 5,
    "max": 300,
    "hint": "图像处理的最大超时时间"
  },
  "output_quality": {
    "description": "输出图像质量",
    "type": "int",
    "default": 85,
    "min": 1,
    "max": 100,
    "hint": "输出JPEG图像的质量 (1-100)，仅对JPG格式有效"
  },
  "enable_gif": {
    "description": "启用GIF处理",
    "type": "bool",
    "default": true,
    "hint": "是否启用GIF动画处理功能"
  },
  "enable_compression": {
    "description": "启用自动压缩",
    "type": "bool",
    "default": true,
    "hint": "是否自动压缩处理后的图像以减小文件大小"
  },
  "silent_mode": {
    "description": "静默模式",
    "type": "bool",
    "default": true,
    "hint": "是否启用静默模式（不发送处理提示消息）"
  },
  "enable_auto_cleanup": {
    "description": "启用自动清理",
    "type": "bool",
    "default": true,
    "hint": "发送成功后自动删除生成的文件"
  },
  "keep_files_hours": {
    "description": "文件保留时间 (小时)",
    "type": "int",
    "default": 1,
    "min": 0,
    "max": 168,
    "hint": "生成的文件保留多少小时后自动删除 (0=立即删除)"
  },
  "enable_at_avatar": {
    "description": "启用@用户头像功能",
    "type": "bool",
    "default": true,
    "hint": "是否允许通过@用户获取其头像进行处理"
  },
  "qqofficial_appid": {
    "description": "QQ官方机器人 AppID (可选)",
    "type": "string",
    "default": "",
    "hint": "仅在 qqofficial / qqofficial_full 适配器下用于头像获取。通常可自动从平台实例读取，无需填写；若自动获取失败可在此手动填入"
  },
  "rate_limit_per_minute": {
    "description": "请求频率限制 (次/分钟)",
    "type": "int",
    "default": 10,
    "min": 0,
    "max": 60,
    "hint": "每个用户每分钟最多能请求多少次镜像对称处理。设为0表示不限制（不推荐）。"
  },
  "max_concurrent_tasks": {
    "description": "并发处理上限",
    "type": "int",
    "default": 3,
    "min": 1,
    "max": 10,
    "hint": "插件同时处理镜像对称图像任务的最大数量"
  },
  "max_gif_frames": {
    "description": "GIF最大帧数",
    "type": "int",
    "default": 200,
    "min": 10,
    "max": 1000,
    "hint": "单个GIF允许处理的最大帧数"
  },
  "cleanup_timeout": {
    "description": "清理任务超时时间 (秒)",
    "type": "float",
    "default": 5.0,
    "min": 1.0,
    "max": 30.0,
    "hint": "插件卸载时等待清理任务结束的最长时间"
  },
  "cleanup_loop_interval": {
    "description": "清理循环间隔 (秒)",
    "type": "int",
    "default": 300,
    "min": 60,
    "max": 1800,
    "hint": "后台定时清理生成文件的检查间隔"
  }
}